name: Shopify Continuous Integration Workflow

on: [push]

jobs:
    build:

        runs-on: ubuntu-latest

        steps:
            -   name: Shopify Module Build
                uses: ballerina-platform/ballerina-action@master
                with:
                    args:
                        build -a -c
                env:
                    shopify_token: ${{ secrets.SHOPIFY_ACCESS_TOKEN }}
